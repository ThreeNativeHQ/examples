# PRD — Midway reference repair

Status: paused at user handoff · Phase 2 of 3. See [MIDWAY-HANDOFF.md](./MIDWAY-HANDOFF.md) for the complete remaining game/model inventory and resumable cockpit session.
Complexity: 6 → MEDIUM; risk override: none. Engine flight and Three shadow patch cross the installed package boundary; supplied animated models add asset integration.

## Objective and scope
Match Midway's shadows, haze and detailed ocean with a 06:30 dawn environment while keeping the existing ThreeNative renderer and flight model. Down pulls the nose up, as requested; releasing the stick must stabilize assisted flight rather than continue pitching into a stall. Fix related reproducible bugs in these paths. No new gameplay systems or platform claims.

## Established facts
- Reference: `/home/joao/projects/threenative/threenative-engine/Midway-Open-Pacific-v3.html`, original screenshot `/tmp/codex-clipboard-gaCBQf.png`.
- Game `src/render/world.ts` disables every shadow and changes original lights, exposure, fog and clouds.
- `src/render/ocean.ts` samples a spectral sea on a uniform grid with 70 m spacing and finite-difference normals over 140 m; the reference has dense concentric geometry, per-pixel normals, sky reflection and filtered ripples.
- `src/scenes/Midway.ts:update` reads inverted pitch and calls `Battle.step(1/60)`; `src/sim/flight.ts:AircraftFlight` delegates to core `FlightModel`.
- Existing scenarios assert console health only; they do not assert successful flight.
- Reuse the active installed game at `../sandbox/midway-open-pacific` and preserve unrelated sandbox files. No worktree created.

### Phase 1 — Repair
- [x] Reproduce stick-release failure, fix its owning layer, and retain a runnable regression. Lane: local, agent. Core missing /4.8 restored; 6/6 flight tests pass, installed consumer settles to vy=0.036 m/s with no stall.
- [x] Capture baseline and restore daylight, working shadows and a detailed, non-shimmering ocean using existing engine capabilities. Lane: local, agent.

### Phase 2 — Supplied models and dawn environment
- [x] Integrate supplied animated Douglas aircraft and Enterprise carrier, with metre scale, correct orientation and deck contact. Lane: local, agent. Blender measurements and model regressions pass; wheel penetration stays within 5cm and no wheel floats.
- [x] Use supplied sky assets and reference water detail, tuned for 06:30 dawn; procedural smoke/fire and restrained muzzle flashes. Lane: local, agent. HDR dawn, filtered ocean reflections and smaller muzzle pulses rendered in browser captures.
- [ ] Match the new cockpit reference with detailed 3D interior; remove persistent mouse steering and verify revised input in the real browser. Lane: local, Opus 5 high + parent.

### Phase 3 — Verify and deliver
- [ ] Run typecheck/build and real browser controls, takeoff, restart and quality-change checks with no runtime/GPU errors. Lane: local, agent.
- [ ] Inspect fresh reference-sized and small-screen captures, record results here and commit only this game's changes (plus engine change if required). Lane: local, agent.

## Verification
Use the installed playtest runner and Playwright through `tools/capture-lock.sh`; real WebGPU adapter required. Browser flight evidence must observe altitude/pitch/stall after releasing input. Engine changes require engine unit and relevant integration checks. Native platforms are outside this browser repair's claims.

## Repair evidence
- `screenshots/repair-before.png`: dark materials, no shadows, flat spectral surface.
- `screenshots/repair-pass3.png`: restored legacy art colour/light units, filtered WaveField ocean; shadows enabled on real NVIDIA/Turing, zero console/GPU errors.
- Engine shadow regression: two wing material groups evicted each other before GPU submit. Source-material cache key fix passes 11 tests, browser confirms corrected shadows.
- User added supplied Douglas GLB with animations, CVN-80 Enterprise GLB and battle-of-pacific photographic sky/ocean reference. Keep inverted pitch. Fire/smoke source textures excluded by request.
- Blender MCP processed the supplied Downloads carrier originals without modifying them: `enterprise.glb` retains 32,209 triangles; `hornet.glb` retains all 347,281 ship triangles and removes all nine identical parked B-25s; `b25-mitchell.glb` exports one reusable specimen with all 25,910 original triangles. Embedded textures remain present, vertex attributes are separate for WebGPU. `node tools/check-carrier-assets.mjs` passes all four asset budgets and verifies the ship no longer contains its parked aircraft.
- Exported carriers face -Z at metre scale: Enterprise 337m long, Hornet 251.58m. Blender raycasts measure deck Y=20.060/20.057; conservative centered launch rectangles are 310×24m and 220×20m, accounting for tapered bows. The supplied Enterprise is modern CVN-80 as explicitly requested; the Hornet source uses Enterprise-1944 mesh names. B-25 extraction and bow orientations inspected in Blender's viewport. Loader typecheck passes; browser integration remains Phase 3 work.
- User cautioned against visible polygon reduction: Blender close/full comparisons (`hornet-original-close.png`, `hornet-optimized-close.png`, and corresponding `-full.png` captures) exposed lost railings, rigging and distorted deck textures at 78k triangles. Rejected that reduction and restored original carrier and B-25 geometry; no speculative LOD added. Triangle reduction had not reduced Hornet's 94 draw calls.
- Added supplied `akagi.glb` through Blender MCP, preserving all 246,563 triangles and 33 embedded images, with no parked aircraft present. Export length260.67m, bow-Z, center deckY20.06; original stern deck slopes down approximately1.4m, so a flat deck collider is only an approximation there. `screenshots/akagi-full.png` visually inspected; loader supports `createCarrier('akagi')` and typecheck passes.
- Close CVN-80 screenshot exposed source limitations: its original and exported material parameters match, with a single1024² hull/deck colour atlas across337m. Replaced the visible main Enterprise with the full-detail supplied WWII sister-ship asset (Enterprise-1944 mesh names, Hornet hull markings retained); main deck now uses that asset's220×20m clear corridor and251.58m visual length. Modern CVN-80 stays available on disk but is no longer downloaded at runtime. Shared carrier texture filtering uses the renderer's measured maximum anisotropy; no texture upsampling or geometry reduction.
- Narrow-deck screenshot checked by Blender raycasts: hero launch position localZ+105 has23.3m actual deck width (1.84 Douglas12.66m spans); localZ+15 has32.4m (2.56 spans), and widest measured section atZ-30 is34.4m. The source's stern taper explains the difference; no nonuniform scaling applied. This is consistent with NHHC's114ft/34.75m extreme-width figure for Enterprise CV-6 ([NHHC ship history](https://www.history.navy.mil/research/histories/ship-histories/danfs/e/enterprise-cv-6-vii.html));40.13m full asset bounds include outboard equipment. Recommended wider-deck briefing composition independently of the tested launch datum.

## Audio evidence
- Inspected every candidate in `/home/joao/projects/threejs-to-bevy/examples/battle-of-pacific/assets/generated/audio/` with `ffprobe`/`ffmpeg volumedetect,astats,silencedetect` before reuse. `machinegun-burst`, `stall-horn` and `water-splash` are byte-identical in size (33,062) but **not** duplicates: their decoded PCM hashes and RMS envelopes all differ; the identical size is CBR arithmetic (2.000 s at 132 kbps). Provenance: that project's `content/audio/pacific-audio.audio.json`.
- `engine-loop.ogg` measured genuinely seamless — head 120 ms and tail 120 ms both at −10.2 dB mean with flat RMS across each (no fade-in or fade-out), peak −1.6 dB. Reused verbatim and looped with plain `loop = true`; no crossfade needed. Independently confirmed at sample level by the supervisor: boundary jump 0.00119 against an ordinary 99th-percentile adjacent delta of 0.01165, an order of magnitude smaller, so the wrap is continuous. `wind-loop.mp3` boundary 0.463 against ordinary 99th-percentile 0.495 — within normal broadband-noise variation.
- `engine-start.ogg`/`engine-stop.ogg` were copied in the first pass but never referenced by `SOUNDS`; using them would need a start/stop state machine this task does not justify. Both **deleted** from `public/assets/audio/`; the upstream originals are untouched. Shipped set is 8 files, 392 KB.
- `machinegun-burst.mp3` carried 1.25 s of sub −43 dB dead tail after its −8.4 dB attack, so it is trimmed to 0.75 s (33 KB → 14 KB). `flak-airburst.mp3` is the quietest source (−25.4 dB mean, −4.9 dB peak, soft attack), so it ships the largest playback gain (0.85) to level against the others; `stall-horn.mp3` is the loudest (−8.7 dB mean) and takes the smallest (0.22).
- `battle-music.mp3` **rejected**: 353 KB, 22 s, −20.3 dB mean. Engine, slipstream and guns already fill the mix in a flight sim and a looping music bed fights the stall horn and radio calls. Not copied; still available upstream if a score is wanted later.
- Three genuinely missing sounds generated with the ElevenLabs `POST https://api.elevenlabs.io/v1/sound-generation` API (key read from `sandbox/.env` as `ELEVENLABS_API_KEY`, never printed; all three HTTP 200), then inspected rather than accepted blind. Request body for every clip was `{"text": <prompt>, "duration_seconds": <d>, "prompt_influence": 0.6}` — `model_id`, `loop` and `output_format` were **not** sent, so each used the endpoint default (the returned bytes decode as 44.1 kHz stereo MP3). Exact prompts:
  - `explosion`, `duration_seconds: 2.5` — "Large naval bomb explosion on a warship deck: deep concussive boom, metal debris, low rumbling tail, distant, no music"
  - `bomb-release`, `duration_seconds: 1.6` — "Heavy aircraft bomb releasing from a dive bomber: a mechanical shackle clunk then a deep falling air whistle rushing away, no music, no voice"
  - `deck-wind` (shipped as `wind-loop.mp3`), `duration_seconds: 6.0` — "Steady open-cockpit slipstream wind rushing past a WW2 aircraft canopy, smooth continuous airflow hiss, no gusts, no music, seamless"
  Post-generation inspection:
  - `explosion.mp3` — accepted as generated: −5.9 dB attack decaying monotonically to −70 dB over 2.48 s.
  - `bomb-release.mp3` — 1.0 s of whistle then dead air; trimmed to 1.15 s.
  - `wind-loop.mp3` — body is dead flat at −15.8 ±0.3 dB across 6 s, but the generator added a fade-out over the final ~350 ms (tail 150 ms at −14.6 dB vs head −12.8 dB) that would dip audibly on every wrap. Trimmed to the flat region [0.30, 5.65]; the seam now measures head −12.8 dB / tail −12.6 dB.
- `src/audio.ts` keeps `start()`, `update(player, paused)`, `event(e)` and `muted`. **`dispose()` is new and the parent is wiring it into `Midway.exit`** — the earlier "no wiring required" note was wrong. The sawtooth/sine drone now survives only as the fallback for a failed fetch or decode and is faded out and freed the moment `engine-loop.ogg` decodes. Added: sample loading, per-event cooldown and voice caps, distance attenuation (flak 2200 m, explosion 3000 m, splash 1800 m), a speed-driven slipstream loop, an edge-triggered stall horn driven from `update()` (no new event type), and `dispose()`. `damage`, `radio` and `land` stay synthetic — no recording ships for them and a radio blip is better served by the oscillator than by a wrong sample.
- `muted` became an accessor pair so the SOUND ON/OFF button drops the master gain on the same tick instead of the next frame; reads and `audio.muted = !audio.muted` are unchanged.
- `node scripts/check-audio.mjs` — 12 checks pass against a stub WebAudio graph, transpiled with the esbuild vite already installs (no dependency added). Covers: single context under repeated gestures, sample loops started exactly once, the drone retiring, cooldown vs voice cap, distance cutoff, immediate mute and pause, stall edge-trigger and re-arm, `dispose()` stopping/disconnecting/closing, a fetch failure reported once with the fallback intact, cutoff silence for flak/explosion/splash on both the sample and fallback paths, mute toggled while paused reopening neither the gain nor any voice, and `dispose()` with a sampled gun voice and a synthetic beep both sounding.
- The checks were validated by negative control rather than trusted for passing: mutating the gun voice cap (4→99), the stall edge trigger, the drone retirement and the `dispose()` close each produce the matching assertion failure. Two checks were found vacuous on the first pass — the stub's clock never advanced and finished one-shots were never retired, so the cooldown masked both the voice cap and the stall edge-trigger; the stub now fires `onended` on buffer end and both assertions bite.
- `pnpm typecheck` clean. `pnpm exec vite build` succeeded in 511 ms on the pre-correction set; the shipped set is now the 8 files in `public/assets/audio/` (392 KB).
- Correction pass (reviewer findings, all fixed and gated):
  1. `explosion` and `splash` used a `Math.max` floor with `near()`, so an event past its falloff still played at the floor. Both now return before any sample **or** fallback once attenuation reaches zero, and the synthetic fallback volume scales with attenuation on all three ranged events — a failed sample load can no longer make a distant hit audible.
  2. The `muted` setter reopened the master even while paused, and `event()` opened voices while paused. `update()` now records the pause state; the setter and every one-shot and fallback trigger respect it.
  3. `dispose()` stopped the loops and the drone but left sounding one-shots running. Live voices are now tracked in a set, stopped and disconnected on dispose, with `onended` cleared so a late callback cannot refill the voice map.
  4. Unused `engine-start`/`engine-stop` assets removed (above).
- Subjective listening: **none occurred, at any point.** No audio-perception tool exists in this environment. Every acoustic claim above is an `ffmpeg`/`ffprobe` measurement or the supervisor's independent sample-level boundary analysis. A browser playtest confirming the samples audibly replace the drone in-game remains outstanding.
- Engine capability search was completed by the supervisor, not blocked: `AudioBus` (create buses before clips and dispose them; `refDistance`/`rolloff` for `playAt`) and `audioPass` (declared loops measured against decoded seam; supports Ogg and WAV, **not** MP3) were found, and all other hits' details read. No engine audio expansion was made in this task — the existing `Soundscape` WebAudio seam predates it and stays bounded to the browser. No native claim is made.

## Current checks
- Core release regression: missing lift-slope division repaired; six flight tests and eleven shadow-material patch tests pass. Full root gates did not pass: publint lost its temporary build directory; a concurrent typecheck encountered generated declarations during rebuild. No native claim.
- `node scripts/check-flight.mjs`: both aircraft survive 60 seconds on the imported 220×20m launch corridor from −55m, neutral bank below 0.14 degrees, bank settles after alternating input. Automatic gear-up waits for sustained safe climb; G overrides for the sortie.
- `node scripts/check-aircraft.mjs`: 12 clips retained, simultaneous surfaces, clear cockpit sightline, working instrument texture and propeller blur; tyre contacts within 5cm at the deck datum.
- `node scripts/check-audio.mjs`: 12 checks pass. Audio inspected numerically, not subjectively auditioned.
- Browser test driver now resolves the exact loaded Vite module URL, including its cache query; importing a fresh unqueried URL could create a second, unstarted game. Final browser checks remain pending the delegated changes.
- Persistent virtual mouse stick removed. It held a commanded deflection at the last cursor offset, so a player using the mouse and the keys together fought their own stale input and the aircraft turned unasked. Mouse-aim games that do work this way (World of Warplanes' hybrid auto-centre, War Thunder's mouse-aim instructor) pair the pointer with an auto-centring instructor this game never had, so the fix is deletion, not a second autopilot. Keyboard is now the only stick — arrows plus A/D, Down still raises the nose; left button held or Space fires; right-button drag still moves the view only. `mouse.x/y/active/cx/cy`, the activation toast, the X release action and the HUD stick marker are gone; the flight-manual and briefing hints now say what the game actually does.
- `pnpm typecheck` clean; `node --check tools/check-repair.mjs` clean; `node scripts/check-flight.mjs` reports `{"pass":true}` with neutral bank on both loadouts. `tools/check-repair.mjs` now asserts mouse movement and left click leave elevator and aileron at zero, that the stick fields no longer exist, that a held left button produces muzzle flashes, that a right drag moves `lookYaw`/`lookPitch` without steering, and that ArrowDown raises the nose and releases to neutral. Browser run not executed here — the parent session holds the final capture.
