node_modules/
dist/
# Regenerated by `pnpm mesh` and `pnpm build:web`: 12.6 MB in and 7.6 MB out, neither of them
# authored. This tree has already come within one command of landing a 700 MB cache.
assets/*.glb
public/face.*.glb
public/assets.manifest.json
artifacts/playtest/capture.json
