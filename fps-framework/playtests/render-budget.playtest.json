{
  "name": "render-budget",
  "target": "web",
  "schemaVersion": 1,
  "viewport": {
    "width": 1280,
    "height": 720
  },
  "warmupFrames": 10,
  "steps": [
    {
      "kind": "wait",
      "label": "settle-effect-pools",
      "waitTicks": 180
    }
  ],
  "assert": {
    "diagnostics": {
      "noConsoleErrors": true,
      "noNetworkErrors": true,
      "noRuntimeDiagnostics": true,
      "runtimeReady": true
    },
    "components": [
      {
        "entity": "render",
        "component": "drawCalls",
        "lte": 500,
        "allowTrivial": "drawCalls is zero until the renderer presents the live town; the Phase 4 budget cuts Bayview's measured 418 indexed submissions rather than hiding the scene."
      },
      {
        "entity": "render",
        "component": "nodeMaterials",
        "lte": 0,
        "allowTrivial": "nodeMaterials counts the live town's unique Three.js node materials; the pre-Phase-4 game had a non-zero TSL graph resolved by the Pixel profile, so zero is the intended bounded-cost material lane."
      },
      {
        "entity": "render",
        "component": "triangles",
        "gte": 300000,
        "allowTrivial": "triangles is zero before a frame renders; retaining at least 300000 prevents meeting the draw budget by removing the authored town."
      }
    ]
  }
}
